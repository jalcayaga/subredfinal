const ButtonAccess = ({ styles }) => (
  <button
    type="button"
    className={`py-1.5 px-2.5 font-poppins  hover:text-secondary cursor-pointer font-medium text-[14px] text-gn border-2 border-gn rounded-[10px] ${styles}`}
  >
    Acceso
  </button>
);

export default ButtonAccess;
